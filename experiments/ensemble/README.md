# Ensemble inference

Configure PyTorch checkpoints or OpenVINO model directories in `config_ensemble.yaml`, then use `infer_ensemble.py` and `evaluate_ensemble.py`. Post-processing includes regular/shifted-window blending, temporal smoothing, and optional power adjustment.
