# Model checkpoints

Place local PyTorch checkpoints under `weights/` (for example `weights/teacher.pt`) and optional OpenVINO `.xml`/`.bin` pairs under `weights/openvino/`.

Weights are excluded from Git because they are large generated artifacts and may have separate distribution terms. Record a download URL, checksum, training configuration, fold, and metric here before sharing a checkpoint externally.
